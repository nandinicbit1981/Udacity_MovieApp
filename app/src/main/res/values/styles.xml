<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>
    
    <style name="BoldText">
        <item name="android:fontFamily">Arial</item>
        <item name="android:paddingBottom">@dimen/title_bottom_padding</item>
        <item name="android:paddingLeft">@dimen/title_left_padding</item>
        <item name="android:paddingTop">@dimen/title_top_padding</item>
        <item name="android:textSize">@dimen/sub_title_textsize</item>
        <item name="android:textStyle">bold</item>
    </style>

</resources>
